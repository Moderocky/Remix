
type thing {

    func Blob string string {
        system.Print { string }
    }

}

type intro {

    func Main {
        object thing { alloc thing }

        thing look {
            func Blob string string
        }

        thing.Blob { "hello" }

        if ?thing = 1 {
            system.Error { "Object error." }
        }

    }

}
